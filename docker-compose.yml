services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: scenepilot
    ports:
      - "5432:5432"
    volumes:
      - scenepilot_postgres_data:/var/lib/postgresql/data

volumes:
  scenepilot_postgres_data:
